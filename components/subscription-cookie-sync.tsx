"use client"

/**
 * Mount-time subscription cookie sync is handled by SubscriptionProvider.
 * This component exists so the layout can include an explicit sync hook in the tree.
 */
export function SubscriptionCookieSync() {
  return null
}
